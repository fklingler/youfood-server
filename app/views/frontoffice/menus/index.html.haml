- model_class = Menu
%h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:name)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @menus.each do |menu|
      %tr
        %td= link_to menu.name, frontoffice_menu_path(menu)
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_frontoffice_menu_path(menu), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), frontoffice_menu_path(menu), :method => :delete, :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')), :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_frontoffice_menu_path, :class => 'btn btn-primary'
