= simple_form_for @menu, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :begin_date, :input_html => { :class => 'date_picker' }
  = f.input :end_date, :input_html => { :class => 'date_picker' }
  = f.button :wrapped, :cancel => menus_path