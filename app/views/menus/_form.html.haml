= simple_form_for @menu, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :product_ids, :collection => Product.all, :as => :check_boxes unless Product.empty?
  = f.button :wrapped, :cancel => menus_path